{
  "name": "ddlabel",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prod": "concurrently \"yarn workspace frontend prod\" \"yarn workspace backend prod\"",
    "start": "concurrently \"yarn workspace frontend start\" \"yarn workspace backend start\"",
    "build": "yarn workspace frontend build && yarn workspace backend build"
  },
  "devDependencies": {
    "@types/papaparse": "^5.3.14",
    "concurrently": "^6.0.0",
    "pm2": "^5.4.1"
  }
}
