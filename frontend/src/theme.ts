import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#FD8F14', // Your custom primary color
		},
		secondary: {
			main: '#914E01', // Your custom secondary color
		},
		info: {
			main: '#A56925', // Your custom secondary color
		},
	},
});

export default theme;
